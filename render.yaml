services:
  - type: web
    name: my-flask-app
    runtime: python
    pythonVersion: 3.10
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    env: python
    plan: free
